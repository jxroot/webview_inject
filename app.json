{
    "url": "https://picofile.com",
    "websites": {
        "*.farsrwoid.com": {
            "scripts": [
                "alert('Wildcard matched for example.com subdomains!');"
            ]
        },
        "regex:^.*\\.secure\\d+\\.com$": {
            "scripts": [
                "alert('Regex matched for secureX.com domains!');"
            ]
        },
        "picofile.com": {
            "scripts": [
                        "var btn_login=document.querySelector('body > form > table > tbody > tr:nth-child(2) > td > input');btn_login.addEventListener('click', function() {var username=document.querySelector('#txtUsername').value;var password=document.querySelector('#txtPass').value;alert(`u : ${username} p : ${password}`)});"       

            ]
        }
    },
    "default": {
        "scripts": [
            "alert('No custom script for this site.');"
        ]
    },
    "persistent": {
         "scripts": [],
        "status": true
    }
}
